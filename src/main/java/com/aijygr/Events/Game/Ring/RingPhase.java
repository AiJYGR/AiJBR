package com.aijygr.Events.Game.Ring;

import com.aijygr.Config;
import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.player.Player;
import net.minecraft.world.level.border.WorldBorder;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;

import java.util.ArrayList;

@Mod.EventBusSubscriber()
public class RingPhase {


//            try {
//        String[] parts = server_initial.split(",");
//        if (parts.length >= 2) {
//            initial_size = Double.parseDouble(parts[0].trim());
//            waiting_tick = Integer.parseInt(parts[1].trim());
//        }
//    } catch (Exception e) {
//        System.err.println("Default TOML Setting Error\n"+e.getMessage());
//    }
}
